<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= fruit.name %></title>
</head>
<body>
    <h1>Edit your <%= fruit.name %></h1>
    <form action="/fruits/<%= fruit._id %>?_method=PUT" method="POST">
        <label for="name">Name of Fruit:</label>
        <input type="text" name="name" id="name" value="<%= fruit.name %>" required>

        <label for="color">Color of Fruit:</label>
        <input type="text" name="color" id="color" value="<%= fruit.color %>" required>

        <label for="readyToEat">Ready to Eat:</label>
        <input type="checkbox" name="readyToEat" id="readyToEat" <%= fruit.readyToEat ? 'checked' : '' %>>

        <button type="submit">Update Fruit</button>
    </form>

    <a href="/fruits">Back to All Fruits</a>
</body>
</html>
